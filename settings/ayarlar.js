require("dotenv").config();

module.exports = {
    TOKEN: process.env.TOKEN || "",  // lrows token
    PREFIX: process.env.PREFIX || "!", // lrows prefix
    SAHİP: process.env.SAHİP || " ", //lrows id
}
